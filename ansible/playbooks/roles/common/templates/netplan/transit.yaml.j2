network:
  version: 2
  ethernets:
    {{ item['name'] }}:
      addresses:
        - {{ item['ip'] }}
      match:
        macaddress: {{ item['mac'] }}
      mtu: {{ networking['mtu'] }}
      nameservers:
        addresses:
          - {{ cloud_ip }}
{% for s in dns['servers'] %}
          - {{s}}
{% endfor %}
