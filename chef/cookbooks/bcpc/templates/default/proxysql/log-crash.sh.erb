#!/bin/sh

###############################################################################
#                           generated by chef
###############################################################################

# This script is executed by ProxySQL when it crashes; it appends an entry to
# the crash.log.

set -e

echo "$(date +"%Y-%m-%d %H:%M:%S") [FATAL] ProxySQL crashed. View <%= node['bcpc']['proxysql']['errorlog'] %> for more details" >> <%= node['bcpc']['proxysql']['datadir'] %>/crash.log
